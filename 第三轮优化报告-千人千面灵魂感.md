# 第三轮优化报告：千人千面灵魂感 + 回复质量提升

## 核心问题诊断

| 问题 | 根因 | 严重程度 |
|------|------|----------|
| 回复像同一个人 | 每个伴侣的"灵魂"被埋在 40+ 节提示深处，模型先看到 14 条规则后才看到"我是谁" | ⚠️ 高 |
| 出现乱码/看不懂 | 13B 小模型在高温(0.7) + 低重复惩罚(1.05) + 长输出(1024 token)下容易退化 | ⚠️ 高 |
| 回复不自然 | 14 条铁律占据模型太多注意力，没空间真正"演"角色 | ⚠️ 中 |
| 词句循环 | repetition_penalty=1.05 太低，模型容易陷入重复 | ⚠️ 中 |

## 修改内容

### 1. 系统提示重构（最核心改动）— `chat-character-prompt.ts`

**旧结构**：40+ 节，角色身份藏在前 15 节规则之后

**新结构**：9 节，角色核心灵魂在**最顶部**
```
1. 你是谁（核心灵魂）← 名字、性格、背景、职业、灵魂世界、说话方式
2. 核心规则       ← 6 条精简（原 14 条）
3. 关系阶段       ← 亲密等级、频道
4. 回复格式       ← 场景/对话模式
5. 关系动态       ← 情侣动态、称呼、剧情设定
6. 上下文         ← 情绪、记忆、里程碑、情景、世界观
7. 外貌与气质     ← 性感特质
8. 外形/穿着/欲望 ← 技术细节
9. 禁止/额外规则   ← 收尾
```

**新增 `buildSoulCore()` 函数**：每个伴侣的独特身份（性格、职业、灵魂世界、说话方式、口吻范例）合并为一个紧凑区块，**放在模型第一个看到的位置**。这才是真正的"千人千面灵魂注入"。

### 2. 生成参数调优 — `llm-service.ts`

| 参数 | 旧值 | 新值 | 为什么 |
|------|------|------|--------|
| temperature | 0.7 | **0.65** | 降低随机性，减少乱码 |
| max_tokens | 1024 | **512** | 强制聚焦，防止退化 |
| repetition_penalty | 1.05 | **1.13** | 大幅减少词句循环 |

### 3. 输出质量增强 — `chat-reply-sanitize.ts`

- **循环检测**：检测同一词重复 4+ 次、短语重复 3+ 次，自动截断
- **叙述泄漏检测**：检测纯第三人称旁白（无对话），标记为垃圾回复
- **早期截断**：检测到循环后自动在第一个干净句子处截断，不等到 3500 字上限

### 4. 输出指令强化 — `stream/route.ts`

- 中文：增加"每条回复必须是自然的中文，有完整的句子"
- 英文：增加"no repeated words or phrases, complete sentences"

## 预期效果

1. **千人千面灵魂感** ✅ — 每个伴侣的独特身份在提示最顶部，模型第一时间看到"我是谁"
2. **更自然的回复** ✅ — 从 14 条规则减到 6 条，模型有更多"脑力"用于自然对话
3. **无乱码** ✅ — 温度 0.65 + 重复惩罚 1.13 + 循环检测 + 早期截断，四重保障
4. **无循环** ✅ — 重复惩罚 + 词句循环检测 + 早期截断

## 验证

所有修改文件通过 TypeScript 语法解析，导出函数签名保持不变，完全向后兼容。